# Бачурин Никита ИТС-2 Лабораторная №6

# Задание 1

## Задача 1

### Текст задачи
Разработать класс с двумя логическими полями. Создать конструктор копирования. Разработать метод, вычисляющий дизъюнкцию полей. Перегрузить метод ToString() для формирования строки из полей класса. Реализовать дочерний класс (его содержание предложить самостоятельно и описать в решении: какой содержательный смысл имеют поля; реализовать конструкторы; предложить и реализовать 2-3 метода). Протестировать все конструкторы и другие методы базового и дочернего классов.

### Алгоритм решения
1\. создаётся класс Device

2\. в классе объявляются два логических поля isOn и hasPower

3\. реализуется конструктор с параметрами для инициализации полей

4\. реализуется конструктор копирования, который копирует значения полей из другого объекта

5\. реализуется метод CanWork

6\. в методе выполняется логическая операция ИЛИ над полями isOn и hasPower

7\. если хотя бы одно из значений истинно, метод возвращает true

8\. если оба значения ложны, метод возвращает false

9\. переопределяется метод ToString для формирования строки с информацией о состоянии устройства

10\. создаётся дочерний класс SmartDevice, который наследуется от класса Device

11\. в дочернем классе объявляется дополнительное логическое поле isConnectedToWifi

12\. реализуется конструктор с параметрами, который вызывает конструктор базового класса

13\. в конструкторе инициализируется поле isConnectedToWifi

14\. реализуется конструктор копирования, который вызывает конструктор копирования базового класса

15\. в конструкторе копируется значение поля isConnectedToWifi

16\. реализуется метод IsOnline

17\. метод возвращает значение поля isConnectedToWifi

18\. реализуется метод IsSmartActive

19\. в методе вызывается метод CanWork базового класса

20\. результат объединяется с полем isConnectedToWifi с помощью логического И

21\. если оба условия истинны, метод возвращает true

22\. иначе возвращается false

23\. реализуется метод ConnectToWifi

24\. в методе устанавливается значение поля isConnectedToWifi равным true

25\. переопределяется метод ToString

26\. в методе вызывается ToString базового класса и добавляется информация о Wi-Fi

27\. в методе Main создаются объекты классов и выполняется вызов их методов для проверки работы программы

### Тестирование
<img width="823" height="698" alt="Image" src="https://github.com/user-attachments/assets/f62ee0cc-8112-467c-b37a-1aa2d824ca7a" />
<img width="779" height="643" alt="Image" src="https://github.com/user-attachments/assets/8b7645c2-0f7d-4a80-9ac0-1a82a419aa34" />

# Задание 2

## Задача 1

### Текст задачи
Определить пересекаются ли заданные отрезки. Результат должен быть типа bool.

### Алгоритм решения
1\. создаётся класс LineSegment

2\. в классе объявляются два вещественных поля x и y

3\. реализуется конструктор по умолчанию, который инициализирует x и y значением 0

4\. реализуется конструктор с параметрами для задания координат отрезка

5\. реализуется конструктор копирования, который копирует значения x и y из другого объекта

6\. создаются свойства X и Y для доступа к полям класса

7\. реализуется метод Intersects, принимающий другой объект LineSegment

8\. в методе определяется начало первого отрезка как минимальное из x и y

9\. определяется конец первого отрезка как максимальное из x и y

10\. определяется начало второго отрезка как минимальное из его координат

11\. определяется конец второго отрезка как максимальное из его координат

12\. проверяется условие непересечения отрезков

13\. если отрезки не пересекаются, возвращается false

14\. если отрезки пересекаются, возвращается true

15\. переопределяется метод ToString для вывода координат отрезка в виде строки

16\. в методе Main создаются объекты класса и выполняется вызов метода Intersects

### Тестирование
<img width="439" height="572" alt="Image" src="https://github.com/user-attachments/assets/cb9339d2-a49e-4349-b170-c2d708ae91a1" />
<img width="415" height="549" alt="Image" src="https://github.com/user-attachments/assets/eb324ea1-c3e2-4791-8b76-6311d3608965" />

# Задание 3

## Задача 1

### Текст задачи
Унарные операции:
! вычислить длину диапазона, результатом должно быть вещественное
число

++ расширить отрезок на 1 вправо и влево

Операции приведения типа:

int (неявная) – результатом является целая часть координаты х;

double(явная) – результатом является координата y.

Бинарные операции:

\- LineSegment целое число (левосторонняя операция, уменьшается
координата х);

\- целое число LineSegment (правосторонняя операция, уменьшается
координата y);

< LineSegment d – результат равен true, если отрезки пересекаются, и false–
в противном случае.

### Алгоритм решения
1\. в классе LineSegment реализуется унарный оператор !

2\. оператор ! вычисляет длину отрезка как модуль разности координат y и x

3\. результат возвращается в виде вещественного числа

4\. реализуется унарный оператор ++

5\. при выполнении операции уменьшается координата x на 1

6\. увеличивается координата y на 1

7\. возвращается изменённый объект отрезка

8\. реализуется неявное преобразование типа к int

9\. возвращается целая часть координаты x

10\. реализуется явное преобразование типа к double

11\. возвращается значение координаты y

12\. реализуется бинарный оператор LineSegment - int

13\. при выполнении операции уменьшается координата x на заданное число

14\. возвращается изменённый объект

15\. реализуется бинарный оператор int - LineSegment

16\. при выполнении операции уменьшается координата y на заданное число

17\. возвращается изменённый объект

18\. реализуется оператор сравнения <

19\. при выполнении операции вызывается метод Intersects

20\. если отрезки пересекаются, возвращается true

21\. если отрезки не пересекаются, возвращается false

22\. реализуется оператор >

23\. возвращается результат, противоположный оператору <

24\. в методе Main создаются объекты и выполняется проверка всех перегруженных операций

### Тестирование
<img width="439" height="572" alt="Image" src="https://github.com/user-attachments/assets/cb9339d2-a49e-4349-b170-c2d708ae91a1" />
<img width="415" height="549" alt="Image" src="https://github.com/user-attachments/assets/eb324ea1-c3e2-4791-8b76-6311d3608965" />
